# 分析: 分析广告组 70711 的消耗趋势：基于 mi_ads_dmp.dwd_ew_ads_show_re...

## 分析目标
分析广告组 70711 的消耗趋势：基于 mi_ads_dmp.dwd_ew_ads_show_res_clk_dld_conv_hi 表的 billing_actual_deduction_price（微美元，需/1e5转美元），输出近30天按天消耗趋势（必要时下钻到按小时），并识别消耗异常波动日期/时段及可能的维度贡献（如 cost_type、app_id、national_id、code_seat_id）。

## 涉及数据表
- mi_ads_dmp.dwd_ew_ads_show_res_clk_dld_conv_hi

## 执行计划

**计划ID**: plan_20260318_141159_8182dfe4
**创建时间**: 2026-03-18 14:11:59
**任务总数**: 4

### 任务列表

#### 1. 数据概览探查 (`task_001`)

- **类型**: sql
- **描述**: 了解数据规模、时间范围、关键字段分布
- **依赖**: 无
- **预期输出**: 数据概览统计报告
- **参数**:
  - sql_template: overview
  - metrics: ['row_count', 'date_range', 'distinct_counts']
  - business_question: 分析广告组 70711 的消耗趋势：基于 mi_ads_dmp.dwd_ew_ads_show_res_clk_dld_conv_hi 表的 billing_actual_deduction_price（微美元，需/1e5转美元），输出近30天按天消耗趋势（必要时下钻到按小时），并识别消耗异常波动日期/时段及可能的维度贡献（如 cost_type、app_id、national_id、code_seat_id）。
  - tables: ['mi_ads_dmp.dwd_ew_ads_show_res_clk_dld_conv_hi']

#### 2. 时间序列分析 (`task_002`)

- **类型**: sql
- **描述**: 按时间维度统计核心指标趋势
- **依赖**: task_001
- **预期输出**: 时间序列数据
- **参数**:
  - sql_template: time_series
  - granularity: daily
  - business_question: 分析广告组 70711 的消耗趋势：基于 mi_ads_dmp.dwd_ew_ads_show_res_clk_dld_conv_hi 表的 billing_actual_deduction_price（微美元，需/1e5转美元），输出近30天按天消耗趋势（必要时下钻到按小时），并识别消耗异常波动日期/时段及可能的维度贡献（如 cost_type、app_id、national_id、code_seat_id）。
  - tables: ['mi_ads_dmp.dwd_ew_ads_show_res_clk_dld_conv_hi']

#### 3. 趋势可视化 (`task_003`)

- **类型**: visualization
- **描述**: 生成趋势图表
- **依赖**: task_002
- **预期输出**: 趋势图表
- **参数**:
  - chart_type: line
  - metrics: []
  - business_question: 分析广告组 70711 的消耗趋势：基于 mi_ads_dmp.dwd_ew_ads_show_res_clk_dld_conv_hi 表的 billing_actual_deduction_price（微美元，需/1e5转美元），输出近30天按天消耗趋势（必要时下钻到按小时），并识别消耗异常波动日期/时段及可能的维度贡献（如 cost_type、app_id、national_id、code_seat_id）。
  - tables: ['mi_ads_dmp.dwd_ew_ads_show_res_clk_dld_conv_hi']

#### 4. 数据质量检查 (`task_004`)

- **类型**: sql
- **描述**: 检查数据完整性、异常值
- **依赖**: task_003
- **预期输出**: 数据质量报告
- **参数**:
  - sql_template: data_quality
  - business_question: 分析广告组 70711 的消耗趋势：基于 mi_ads_dmp.dwd_ew_ads_show_res_clk_dld_conv_hi 表的 billing_actual_deduction_price（微美元，需/1e5转美元），输出近30天按天消耗趋势（必要时下钻到按小时），并识别消耗异常波动日期/时段及可能的维度贡献（如 cost_type、app_id、national_id、code_seat_id）。
  - tables: ['mi_ads_dmp.dwd_ew_ads_show_res_clk_dld_conv_hi']

## 执行说明

1. 任务按上述顺序依次执行
2. 有依赖的任务需等待前置任务完成
3. 每个任务完成后会更新状态和结果
4. 最终生成综合分析报告

## 状态跟踪

| 任务ID | 任务名称 | 状态 | 结果摘要 |
|--------|----------|------|----------|
| task_001 | 数据概览探查 | pending | - |
| task_002 | 时间序列分析 | pending | - |
| task_003 | 趋势可视化 | pending | - |
| task_004 | 数据质量检查 | pending | - |
