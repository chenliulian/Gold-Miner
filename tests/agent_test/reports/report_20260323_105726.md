# 广告主 2368 近 7 天广告组 CTR/CVR 趋势分析报告

## 总结
- 分析周期为最近 7 天（具体日期范围需根据实际数据确认），聚焦广告主 2368 在 `com_cdm.dws_tracker_ad_cpc_cost_hi` 表中的表现  
- 核心指标为各广告组的 **CTR（点击率）** 和 **CVR（转化率）** 趋势  
- **关键发现**：当前查询未返回有效数据，需进一步验证数据完整性  
- 潜在问题可能涉及数据延迟、广告组活跃度不足或筛选条件偏差  
- 建议优先排查数据源状态及广告主近期投放策略  

---

## 关键 SQL 发现
```sql
-- 示例查询逻辑（实际结果为空）
SELECT 
  ad_group_id,
  dt AS date,
  SUM(clicks) * 1.0 / SUM(impressions) AS CTR,
  SUM(conversions) * 1.0 / SUM(clicks) AS CVR
FROM com_cdm.dws_tracker_ad_cpc_cost_hi
WHERE advertiser_id = 2368
  AND dt >= DATE_SUB(CURRENT_DATE, 7)
GROUP BY ad_group_id, dt
ORDER BY ad_group_id, dt;
```

| ad_group_id | date       | CTR   | CVR   |
|-------------|------------|-------|-------|
| *无数据*    | *无数据*   | *无*  | *无*  |

---

## 异常与模式
1. **数据缺失**：连续 7 天无记录，可能原因包括：  
   - 广告主 2368 近期未投放广告  
   - 数据管道延迟或表分区异常  
   - `advertiser_id` 字段值不匹配（如存在大小写/格式差异）  
2. **指标计算风险**：若分母（曝光量/点击量）为 0，可能导致 CTR/CVR 计算错误（当前无数据未触发此问题）  

---

## 后续步骤
1. **数据验证**  
   - 检查 `com_cdm.dws_tracker_ad_cpc_cost_hi` 表最近 7 天是否有其他广告主数据  
   - 确认 `advertiser_id=2368` 是否存在拼写或映射错误  
2. **业务侧确认**  
   - 联系运营团队核实广告主 2368 近期是否暂停投放  
   - 检查广告组状态（是否被禁用/预算耗尽）  
3. **扩展分析**  
   - 若数据恢复，增加维度：分设备/地域的 CTR/CVR 对比  
   - 关联成本数据（CPC）评估投放效率  

> 注：本报告基于当前空结果生成，需补充数据后重新分析。