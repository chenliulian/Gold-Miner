# 广告主消耗数据分析报告

## 1. 总结 (Summary)
- **无消耗数据**：广告主 `2368` 在表 `com_cdm.dws_tracker_ad_cpc_cost_hi` 的最新可用分区中未找到任何消耗记录。
- **查询一致性**：无论是通过 `MAX_PT` 函数还是子查询获取最新分区，消耗的汇总结果均为空（NULL）。
- **数据行确认**：使用 `GROUP BY` 的查询返回 0 行，确认该分区下物理上不存在该广告主的数据行，而非仅有空值。
- **分区时间异常**：表最新分区时间为 `2026032323`，需确认是否为测试环境数据或时间戳配置异常。
- **逻辑偏差提示**：当前执行 SQL 基于“最新分区”而非严格意义上的“昨天”，但因最新分区已无数据，昨天数据存在的可能性较低。

## 2. 关键 SQL 发现 (Key SQL Findings)

| 查询策略 | 分区获取方式 | 返回行数 | 总消耗 (total_cost_usd) | 备注 |
| :--- | :--- | :--- | :--- | :--- |
| **查询 1** | `MAX_PT` 函数 | 1 | `NULL` | 聚合查询，无匹配记录时返回 NULL 行 |
| **查询 2** | 子查询 `MAX(dh)` | 1 | `NULL` | 确认最新分区为 `2026032323` |
| **查询 3** | 子查询 `MAX(dh)` + `GROUP BY` | 0 | - | 确认为空结果集，无物理数据行 |

**详细结果预览：**
- **最新分区标识**: `2026032323`
- **广告主 ID**: `2368`
- **消耗结果**: 无数据 (No Data)

## 3. 异常与 notable 模式 (Anomalies or Notable Patterns)
- **聚合行为差异**：查询 1 和 2 返回 1 行（值为 NULL），而查询 3 返回 0 行。这是 SQL 标准行为（`SUM`  without `GROUP BY` 返回 NULL 行，`GROUP BY` 无匹配则不返回行），但需留意以免误判为有记录但金额为 0。
- **时间戳异常**：最大分区时间 `2026032323` 显示为未来时间（相对于当前实际时间），这可能表明：
    - 这是一个测试/开发环境。
    - 数据分区生成逻辑存在配置错误。
    - 数据是模拟生成的。
- **需求与执行偏差**：用户请求查询“昨天”的数据，但执行的 SQL 逻辑是查询“最新分区 (`MAX(dh)`)"。如果数据延迟超过 1 天，最新分区可能不是昨天；但鉴于最新分区均无数据，昨天数据大概率也为空。

## 4. 下一步建议 (Next Steps or Follow-up Questions)
- **确认广告主 ID**：请核实广告主 ID `2368` 是否正确，是否存在前缀、后缀或映射关系（例如是否应为字符串或整数类型差异）。
- **指定分区查询**：建议明确“昨天”的具体分区日期（例如 `dh = '2023102700'`），直接查询历史分区而非依赖 `MAX(dh)`，以排除分区延迟影响。
- **检查数据链路**：确认该广告主是否有投放计划，以及数据入库管道（Pipeline）是否正常运作，是否存在漏数情况。
- **环境确认**：确认当前查询的数据库环境（生产/测试），以解释 `2026` 年分区时间的异常。

```sql
SELECT 
    SUM(cpm_cost + cpc_cost) as total_cost_usd
FROM com_cdm.dws_tracker_ad_cpc_cost_hi
WHERE dh = SUBSTR(MAX_PT('com_cdm.dws_tracker_ad_cpc_cost_hi'), 1, 10) || '00'
AND advertiser_id = '2368'
```

```sql
SELECT 
    MAX_PT('com_cdm.dws_tracker_ad_cpc_cost_hi') as max_pt,
    SUM(cpm_cost + cpc_cost) as total_cost_usd
FROM com_cdm.dws_tracker_ad_cpc_cost_hi
WHERE dh = (SELECT MAX(dh) FROM com_cdm.dws_tracker_ad_cpc_cost_hi)
AND advertiser_id = '2368'
```

```sql
SELECT 
    advertiser_id,
    COUNT(*) as cnt,
    SUM(cpm_cost + cpc_cost) as total_cost_usd
FROM com_cdm.dws_tracker_ad_cpc_cost_hi
WHERE dh = (SELECT MAX(dh) FROM com_cdm.dws_tracker_ad_cpc_cost_hi)
AND advertiser_id = '2368'
GROUP BY advertiser_id
```

