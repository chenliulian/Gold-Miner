# 广告主 2368 周消耗数据对比分析报告

## 1. 总结 (Summary)
- **分析目标**：对比广告主 2368 在“上上周”与“上周”期间的消耗数据变化。
- **数据源**：指定数据表为 `com_cdm.dws_tracker_ad_cpc_cost_hi`。
- **查询结果**：本次查询未返回任何有效数据记录（`results_summary` 为空）。
- **当前状态**：由于缺乏基础数据，无法计算环比增长率或评估消耗趋势。
- **主要结论**：需优先排查数据缺失原因，而非进行业务趋势分析。

## 2. 关键 SQL 发现 (Key SQL Findings)
基于查询逻辑，预期应产出分周的消耗汇总，但实际执行结果为空。

| 周期 | 消耗金额 (Cost) | 环比变化 (WoW) | 数据状态 |
| :--- | :--- | :--- | :--- |
| 上上周 | - | - | **无数据** |
| 上周 | - | - | **无数据** |

- **查询逻辑**：`SELECT week, SUM(cost) FROM ... WHERE advertiser_id = 2368 GROUP BY week`
- **返回行数**：0
- **涉及字段**：`advertiser_id`, `cost`, `partition_date` (隐含)

## 3. 异常与 notable 模式 (Anomalies or Notable Patterns)
- **数据缺失异常**：在核心消耗表中完全找不到该广告主的记录，这通常不符合常规活跃广告主的行为模式。
- **潜在原因推测**：
  1.  **广告主 ID 错误**：2368 可能不是有效的广告主 ID，或已变更。
  2.  **投放暂停**：该广告主在这两周内可能完全没有进行投放。
  3.  **数据延迟/故障**：ETL 任务可能未及时更新该表分区，或存在数据丢失。
  4.  **表结构变更**：`com_cdm.dws_tracker_ad_cpc_cost_hi` 表可能存在字段映射问题或权限限制。

## 4. 下一步建议 (Next Steps)
- **验证 ID 有效性**：在广告管理后台确认广告主 2368 是否存在且状态正常。
- **检查数据链路**：联系数据工程团队确认 `com_cdm.dws_tracker_ad_cpc_cost_hi` 表最近两天的数据产出是否正常。
- **扩大查询范围**：
  - 尝试查询更长的时间范围（如过去 30 天），确认是否有历史数据。
  - 尝试查询其他相关表（如 `dws_tracker_ad_impression`），确认是否有曝光但无消耗。
- **确认筛选条件**：检查是否有额外的过滤条件（如特定渠道、特定计划类型）导致数据被排除。